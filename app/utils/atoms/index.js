import { atom } from 'jotai';

export const yearAtom = atom(2024);
export const driverAtom = atom(null);
export const constructorAtom = atom(null);

export const driverStandingsAtom = atom(null);
export const constructorStandingsAtom = atom(null);

export const driversListAtom = atom([]);
export const constructorsListAtom = atom([]);
